[tasks.image]
command = "rust-objcopy"
args = ["--strip-all", "-O", "binary", "target/aarch64-unknown-none/${@}/lunaris", "kernel.img"]
dependencies = ["build"]

[tasks.build]
command = "cargo"
args = ["build", "--${@}"]
